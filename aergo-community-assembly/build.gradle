apply plugin: 'distribution'

dependencies {
  implementation project(':aergo-community-api')
}

distributions {
  main {
    baseName = rootProject.name
    contents {
      from {'src/assembly'}
      into('lib') {
        from (project.configurations.runtimeClasspath)
      }
    }
  }
}